<resources>

    <style name="AppTheme" parent="Theme.AppCompat.Light.NoActionBar">
        <item name="windowActionBar">false</item>
        <item name="android:windowActionBar">false</item>
        <item name="android:textColorPrimary">@color/indigo_600</item>
        <item name="colorPrimaryDark">@color/yellow_600</item>
        <item name="colorPrimary">@color/yellow_400</item>
        <item name="windowActionModeOverlay">true</item>

        <item name="android:windowEnterTransition">@android:transition/slide_top</item>
        <item name="android:windowReturnTransition">@android:transition/slide_bottom</item>
        <item name="colorControlActivated">@color/blue_grey_400</item>
        <item name="android:alertDialogTheme">@style/CustomLollipopDialogStyle</item>

        <item name="actionModeStyle">@style/ActionMode</item>
    </style>

    <style name="ActionMode" parent="@style/Widget.AppCompat.ActionMode">
        <item name="background">@color/yellow_200</item>
    </style>

    <style name="colorControlLightTheme">
        <item name="colorControlActivated">@color/blue_grey_500</item>
    </style>

    <style name="CustomLollipopDialogStyle" parent="Theme.AppCompat.Light.Dialog.Alert">
        <!-- Used for the buttons -->
        <item name="android:colorAccent">@color/blue_grey_800</item>
        <!-- Used for the title and text -->
        <item name="android:textColorPrimary">@color/blue_grey_800</item>
        <!-- Used for the background -->
        <item name="android:buttonBarButtonStyle">@style/CustomButtonBarButtonStyle</item>
        <item name="android:buttonBarStyle">@style/CustomButtonBarStyle</item>
    </style>

    <style name="CustomButtonBarStyle" parent="Widget.AppCompat.ButtonBar.AlertDialog">
        <item name="android:layout_width">match_parent</item>
        <item name="android:layout_height">wrap_content</item>
        <item name="android:height">@null</item>
        <item name="android:minHeight">@null</item>
    </style>

    <style name="CustomButtonBarButtonStyle" parent="Widget.AppCompat.Button.Borderless.Colored">
        <item name="android:layout_width">0dp</item>
        <item name="android:layout_weight">1</item>
        <!-- TODO: setting a fixed width as follows results in narrow buttons with line breaks, but of course this is not a solution -->
        <item name="android:width">100dp</item>
    </style>
</resources>
