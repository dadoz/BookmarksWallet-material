<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.NoActionBar">
        <!-- Customize your theme here. -->
        <item name="windowActionBar">false</item>
        <item name="android:windowActionBar">false</item>
        <item name="android:textColorPrimary">@color/material_violet_500</item>
        <item name="android:textColor">@color/material_violet_500</item>
        <item name="colorPrimary">@color/material_red</item>
        <item name="colorControlActivated">@color/material_violet_500</item>
        <item name="actionModeStyle">@style/ActionMode</item>

        <item name="android:alertDialogTheme">@style/CustomLollipopDialogStyle</item>
    </style>

    <style name="ActionMode" parent="@style/Widget.AppCompat.ActionMode">
        <item name="background">@color/material_red</item>
    </style>

    <style name="colorControlLightTheme">
        <item name="colorControlActivated">@color/material_grey_200</item>
    </style>

    <style name="CustomLollipopDialogStyle" parent="Theme.AppCompat.Light.Dialog.Alert">
        <!-- Used for the buttons -->
        <item name="colorAccent">@color/material_violet_500</item>
        <!-- Used for the title and text -->
        <item name="android:textColorPrimary">@color/material_violet_500</item>
        <!-- Used for the background -->
        <!--<item name="android:background">#4CAF50</item>-->
    </style>
    <style name="CustomButtonBarStyle" parent="Widget.AppCompat.ButtonBar.AlertDialog">
        <!-- Making sure, the button bar uses parent width and is not restricted in height -->
        <item name="android:layout_width">match_parent</item>
        <item name="android:layout_height">wrap_content</item>
        <item name="android:height">@null</item>
        <item name="android:minHeight">@null</item>
    </style>

    <style name="CustomButtonBarButtonStyle" parent="Widget.AppCompat.Button.Borderless.Colored">
        <!-- Setting the weight as follows should result in equally wide buttons filling the alert dialog width,
            but instead they span further out of the dialog, breaking in multiple lines though -->
        <item name="android:layout_width">0dp</item>
        <item name="android:layout_weight">1</item>
        <!-- TODO: setting a fixed width as follows results in narrow buttons with line breaks, but of course this is not a solution -->
        <item name="android:width">100dp</item>
    </style>

    <style name="BottomSheet.StyleDialog" parent="BottomSheet.Dialog">
        <item name="android:backgroundDimAmount">0.5</item>
        <item name="android:windowAnimationStyle">@style/BottomSheet.Animation</item>
        <item name="android:textColorPrimary">@color/material_violet_500</item>
        <item name="android:textColorSecondary">@color/material_violet_500</item>
        <item name="android:textColorHint">@color/material_violet_700</item>
        <item name="bs_dialogBackground">@color/white</item>
        <item name="bs_dividerColor">@color/abc_search_url_text_pressed</item>
        <item name="bs_numColumns">2</item>
        <item name="bs_listStyle">@style/BottomSheet.StyleList</item>
    </style>

    <style name="BottomSheet.StyleList" parent="BottomSheet.List.Dark" >
        <item name="android:listSelector">@drawable/list_selector</item>
        <!--<item name="android:layoutAnimation">@anim/bs_list_layout_fade_in</item>-->
    </style>

</resources>
